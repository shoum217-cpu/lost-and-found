# Custom React Hooks
#
# Place reusable hooks here as the project grows.
#
# Examples to build next:
#   useAuth.js     – subscribes to supabase.auth.onAuthStateChange()
#   useItems.js    – fetches and caches item lists
#   useSearch.js   – debounced search with filter state
